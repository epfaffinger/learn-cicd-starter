package getapikey

import (
    "reflect"
    "testing"
)

func GetAPIKey(t *testing.T) {
    got := GetAPIKey("header1")
    want := []string{"a"}
    if !reflect.DeepEqual(want, got) {
         t.Fatalf("expected: %v, got: %v", want, got)
    }
}
